# Architecture

- Backstage is the developer portal/control plane.
- This repository owns reusable Software Templates/golden paths.
- Each runtime initially gets its own independent template.
- Infrastructure templates will be introduced later.
